#pragma once

#include <Arduino.h>
#include <MACROS.h>

void stackMat(){
    if (LEFT_SENSOR && RIGHT_SENSOR ==)
};

float solveTime (){
    float startSolve = millis();
    float endSolve;
    float solve = endSolve - startSolve;

    return solve;
};